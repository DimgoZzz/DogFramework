#include "DogFW/Math/Integer/DogInt.h"

#include <cmath>
//#include <cstdlib>

namespace DogFW::math
{
	Int8 abs(Int8 inV)
	{
		return std::abs(inV);
	}
	Int16 abs(Int16 inV)
	{
		return std::abs(inV);
	}
	Int32 abs(Int32 inV)
	{
		return std::abs(inV);
	}
	Int64 abs(Int64 inV)
	{
		return std::abs(inV);
	}
}